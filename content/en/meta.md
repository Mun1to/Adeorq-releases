# Metadata and sharing · English

For the `<head>`. The English `title` already lives in `TITULOS.en` inside `js/boot.js`.

## title
Adeorq · the vibe coding workshop on real terminals

## description
Adeorq is the Windows desktop panel where your agents work in real terminals: your projects one
click away, your Claude Code sessions with their actual state, an Agenda for loose ideas and
updates that arrive on their own.

## og.title
Adeorq · nine agents working, one single screen

## og.description
The Windows desktop panel where your agents work in real terminals. Free, no account, no API
keys.

## Notes

- Share image: same asset as the Spanish one, the headline on it stays in English only if we
  ever serve two images. One image is enough for now.
- No `og:url` and no canonical until `adeorq.com` actually exists.
